# Brand Camera 项目开发规则

> **重要**: 开发前请先阅读 [ARCHITECTURE.md](./ARCHITECTURE.md) 了解系统架构和标准实现模式。

## 新功能开发检查清单

每次开发新功能时，必须检查以下模块是否需要同步修改：

### 1. 💰 计费系统
- [ ] `src/app/api/quota/reserve/route.ts` - 扣费逻辑
- [ ] `src/lib/taskTypes.ts` - 任务类型定义（新增类型需要在这里注册）
- [ ] 确认 credit 消耗规则：**图片 1 credit，视频 10 credits**

### 2. 📦 资产管理 & Gallery
- [ ] `src/app/api/gallery/route.ts` - Gallery API 过滤逻辑（新 task_type 需要添加过滤条件）
- [ ] `src/app/(main)/gallery/page.tsx` - Gallery 页面 tab 显示
- [ ] `src/components/shared/GalleryPreloader.tsx` - **PRELOAD_TABS 配置**（新增 tab 必须添加）
- [ ] `src/stores/galleryStore.ts` - 缓存 key 生成逻辑（如有特殊需求）
- [ ] 数据库字段：**必须使用 `output_image_urls`**（不是 `image_urls`）

### 3. 🏷️ 类型系统（重要！）
- [ ] `src/lib/taskTypes.ts` - **TaskTypes 常量** + **TYPE_MAP 映射** + **isXxxType() 函数**
- [ ] `src/types/index.ts` - **GenerationType 联合类型**（添加新类型变种）
- [ ] `src/stores/generationTaskStore.ts` - **TaskType 类型**（如果需要前端任务追踪）

**代码规范：**
```typescript
// ✅ 正确：写入数据库时使用常量
import { TaskTypes } from '@/lib/taskTypes'
task_type: TaskTypes.BRAND_STYLE

// ✅ 正确：判断类型时使用函数（自动兼容历史数据）
import { isBrandStyleType } from '@/lib/taskTypes'
if (isBrandStyleType(item.task_type)) { ... }

// ❌ 错误：直接使用字符串
task_type: 'brand_style'
if (item.task_type === 'brand_style') { ... }
```

### 4. 🎨 UI 一致性 & PC 端适配（重要！）

**⚠️ 每个页面的每个模式（mode）都必须检查 PC 端适配！**

- [ ] 使用 `useIsDesktop` hook + `ScreenLoadingGuard` 组件**防止页面闪烁**
- [ ] 底部导航 `BottomNav` **仅在移动端显示**：`{!isDesktop && <BottomNav />}`
- [ ] 相机功能 **仅在移动端可用**

**🔴 PC 端适配检查清单（每个 mode 都要检查！）：**

| 模式 | PC 端必须检查项 |
|------|----------------|
| camera/main | Header 居中标题、无相机按钮、上传区域合理尺寸 |
| review | 三栏布局、无底部导航 |
| processing | 骨架图居中、max-w 限制 |
| **results** | **4列网格、居中按钮、无底部导航** |

```tsx
// ✅ 正确：results 模式必须有 isDesktop 判断
{mode === "results" && (
  <div>
    {/* Header */}
    {isDesktop ? (
      <div className="max-w-5xl mx-auto px-8 py-4">...</div>
    ) : (
      <div className="h-14 px-4">...</div>
    )}
    
    {/* Content */}
    <div className={isDesktop ? 'max-w-4xl mx-auto' : ''}>
      <div className={`grid gap-4 ${isDesktop ? 'grid-cols-4' : 'grid-cols-2'}`}>
        {/* images */}
      </div>
    </div>
    
    {/* Button */}
    {isDesktop ? (
      <div className="mt-8 flex justify-center">
        <button className="px-8 h-12">...</button>
      </div>
    ) : (
      <div className="p-4 pb-20">
        <button className="w-full h-12">...</button>
      </div>
    )}
  </div>
)}

// ❌ 错误：results 模式没有 isDesktop 判断
{mode === "results" && (
  <div className="p-4">  {/* 移动端样式直接用于 PC */}
    <div className="grid grid-cols-2">...</div>
    <button className="w-full">...</button>
  </div>
)}
```

**参考实现：**`pro-studio/page.tsx` 的 results 模式是标准实现

### 5. 🌍 国际化（重要！禁止硬编码文本）

**⚠️ 绝对禁止在 JSX 中直接写中文/英文/韩文文本！**

- [ ] `src/locales/zh.ts` - 中文翻译
- [ ] `src/locales/en.ts` - 英文翻译  
- [ ] `src/locales/ko.ts` - 韩文翻译
- [ ] **所有用户可见文本**必须使用 `t.xxx` 翻译 key
- [ ] **AI 生成的文本**需要根据用户语言设置生成对应语言

**代码规范：**
```tsx
// ❌ 错误：硬编码文本
<h1>专业棚拍模式</h1>
<p>智能模特匹配</p>
<button>上传</button>

// ✅ 正确：使用翻译 key
<h1>{t.proStudio?.title}</h1>
<p>{t.proStudio?.smartModelMatch}</p>
<button>{t.common?.upload}</button>

// ⚠️ 可接受但不推荐：fallback 应该用英文（作为默认语言）
<h1>{t.proStudio?.title || 'Pro Studio Mode'}</h1>
```

**🚨 自动检测工具：**
```bash
# 开发时检测（只警告）
npm run check-i18n         # 快速扫描
npm run check-i18n:strict  # 详细报告

# CI 模式（发现问题阻止构建）
npm run check-i18n:ci      # 用于 CI/CD pipeline
```

**检测重点：**
- ❌ 按钮文本：`<button>去修图</button>`
- ❌ span 文本：`<span>图片 1</span>`
- ❌ JSX 直接文本：`<div>专业棚拍</div>`
- ✅ 忽略：admin 页面、错误消息、调试信息、注释

**⚠️ 建议在 CI 中添加 `npm run check-i18n:ci` 以阻止硬编码中文进入生产环境！**

**添加新翻译 key 的流程：**
1. 在 `src/locales/zh.ts` 中添加中文
2. 在 `src/locales/en.ts` 中添加英文
3. 在 `src/locales/ko.ts` 中添加韩文
4. 在组件中使用 `t.xxx.yyy`

```typescript
// 获取用户语言并传递给后端
const { language } = useTranslation()
await fetch('/api/xxx', {
  body: JSON.stringify({ ...data, language })
})

// 后端 VLM/LLM prompt 中指定语言
const prompt = `请用${language === 'en' ? '英文' : language === 'ko' ? '韩文' : '中文'}输出...`
```

### 6. 🔍 SEO（新增公开页面时必须维护）

**⚠️ 新增公开页面（不需要登录的页面）时，必须同步更新 SEO 配置！**

- [ ] `src/app/sitemap.ts` - **PUBLIC_PAGES 数组**（添加新的公开页面）
- [ ] `public/robots.txt` - **Allow 规则**（如果是新的公开路径）
- [ ] 页面 `layout.tsx` 或 `page.tsx` - **添加 metadata**

**代码示例：**
```typescript
// src/app/sitemap.ts - 添加新的公开页面
const PUBLIC_PAGES = [
  { path: '', changeFrequency: 'weekly', priority: 1.0 },
  { path: '/login', changeFrequency: 'monthly', priority: 0.8 },
  { path: '/pricing', changeFrequency: 'weekly', priority: 0.9 },
  // 📌 新增公开页面在此添加：
  { path: '/about', changeFrequency: 'monthly', priority: 0.7 },
]

// 新页面的 layout.tsx - 添加 metadata
import { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'About | Brand Camera',
  description: '页面描述...',
}
```

**🚨 自动检测工具：**
```bash
# 开发时检测（只警告）
npm run check-seo

# CI 模式（发现错误阻止构建）
npm run check-seo:ci

# 同时检测 i18n 和 SEO
npm run check-all
npm run check-all:ci
```

**检测内容：**
- ✅ sitemap.ts 是否存在且包含公开页面
- ✅ robots.txt 是否存在且配置正确
- ✅ 公开页面是否有 metadata
- ✅ JSON-LD 结构化数据是否配置
- ✅ OG Image 是否存在

**SEO 相关文件：**
| 文件 | 用途 | 何时更新 |
|------|------|----------|
| `src/app/sitemap.ts` | 站点地图 | 新增公开页面 |
| `public/robots.txt` | 爬虫规则 | 新增公开/私有路径 |
| `src/app/opengraph-image.tsx` | OG 图片 | 品牌视觉变更 |
| `src/components/seo/JsonLd.tsx` | 结构化数据 | 很少更新 |

### 7. 🧭 导航
- [ ] `src/components/layouts/Sidebar.tsx` - PC 端侧边栏 navItems
- [ ] `src/app/(main)/app/page.tsx` - **首页入口卡片**（新功能需要添加入口）
- [ ] 路由：功能页面放在 `src/app/(main)/` 下

### 8. 🔐 认证
- [ ] 页面需要登录检查（layout 已处理）
- [ ] API 路由使用 `requireAuth()` 中间件

```typescript
import { requireAuth } from '@/lib/auth'

export async function POST(request: NextRequest) {
  const authResult = await requireAuth(request)
  if ('response' in authResult) return authResult.response
  const { user } = authResult
  // ...
}
```

### 9. 📝 数据存储
- [ ] 生成结果保存到 `generations` 表
- [ ] 使用 `task_type` 字段区分功能类型
- [ ] 图片上传到 Supabase Storage `generations` bucket
- [ ] 视频需要确认 bucket 已开启 `video/mp4` MIME 类型支持

### 10. 🤖 AI Prompts
- [ ] `src/prompts/index.ts` - 通用 prompts
- [ ] `src/prompts/lifestyle.ts` - lifestyle 专用 prompts
- [ ] 新功能可能需要添加专用 prompt 文件

---

## 项目架构概览

```
src/
├── app/
│   ├── page.tsx              # Landing Page (未登录)
│   ├── (main)/               # 需要登录的页面
│   │   ├── app/page.tsx      # App 首页（入口卡片）
│   │   ├── gallery/          # 作品集
│   │   ├── brand-style/      # Clone Brand Style
│   │   ├── camera/           # 拍照功能
│   │   └── ...
│   └── api/                  # API 路由
│       ├── quota/            # 计费
│       ├── gallery/          # 作品集
│       └── brand-style/      # Brand Style APIs
├── components/
│   ├── layouts/              # 布局组件 (Sidebar, TopNav, BottomNav)
│   ├── shared/               # 共享组件 (GalleryPreloader, QuotaIndicator)
│   └── ui/                   # UI 组件 (ScreenLoadingGuard)
├── hooks/
│   ├── useIsMobile.ts        # useIsDesktop hook（PC/Mobile 判断）
│   └── useQuota.ts           # 额度检查 hook
├── lib/
│   ├── auth.ts               # requireAuth 认证中间件
│   ├── taskTypes.ts          # 任务类型定义（重要！）
│   └── supabase/             # Supabase 客户端
├── stores/                   # Zustand 状态管理
│   ├── galleryStore.ts       # Gallery 缓存
│   ├── generationTaskStore.ts # 生成任务状态
│   └── languageStore.ts      # 语言设置
├── locales/                  # 国际化 (zh, en, ko)
├── prompts/                  # AI Prompts
└── types/
    └── index.ts              # TypeScript 类型定义
```

## 任务类型注册完整流程

新增功能类型时，必须在以下**所有**位置注册：

### Step 1: `src/lib/taskTypes.ts`
```typescript
// 1. 添加常量
export const TaskTypes = {
  // ... existing
  NEW_TYPE: 'new_type',
} as const

// 2. 添加映射（包括可能的变种）
const TYPE_MAP: Record<string, CanonicalTaskType> = {
  // ... existing
  'new_type': TaskTypes.NEW_TYPE,
  'newtype': TaskTypes.NEW_TYPE,  // 可能的变种
}

// 3. 添加判断函数
export function isNewType(type?: string): boolean {
  return getCanonicalType(type) === TaskTypes.NEW_TYPE
}

// 4. 更新 getTypeDisplayName
export function getTypeDisplayName(type?: string): string {
  switch (canonical) {
    // ... existing
    case TaskTypes.NEW_TYPE: return '新功能名称'
  }
}

// 5. 如果需要在 Gallery 模特分类中显示，更新 isModelRelatedType
```

### Step 2: `src/types/index.ts`
```typescript
export type GenerationType = 
  | // ... existing
  | 'new_type'  // 添加新类型
```

### Step 3: `src/app/api/gallery/route.ts`
```typescript
} else if (type === 'newtab') {
  query = query.or('task_type.eq.new_type')
}
```

### Step 4: `src/components/shared/GalleryPreloader.tsx`
```typescript
const PRELOAD_TABS = [
  // ... existing
  { tab: 'newtab', subType: '' },
]
```

### Step 5: `src/app/(main)/gallery/page.tsx`
```typescript
// 添加 tab 定义和显示逻辑
```

---

## 常见问题 Q&A

### Q: 图片在 Gallery 中不显示？
检查：
1. 保存时使用 `output_image_urls` 字段（不是 `image_urls`）
2. `task_type` 已在 `gallery/route.ts` 中添加过滤条件
3. `GalleryPreloader.tsx` 中包含该 tab 的预加载配置

### Q: 页面进入时闪烁/刷新？
使用 `useIsDesktop` + `ScreenLoadingGuard`（见上方示例）

### Q: 切换 tab 时重新请求？
检查 `GalleryPreloader.tsx` 的 `PRELOAD_TABS` 是否包含该 tab

### Q: AI 输出语言不对？
在后端 API 中传递 `language` 参数，并在 VLM/LLM prompt 中指定语言

### Q: 计费没有生效？
1. 检查 `quota/reserve` API 是否被调用
2. 检查 `taskType` 参数是否正确传递
3. 视频任务使用 10 credits，图片使用 1 credit

---

## 环境变量（必需）

```env
# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=  # 注意：不是 SUPABASE_SERVICE_KEY

# AI Models
GOOGLE_AI_API_KEY=
TOGETHER_API_KEY=

# Optional
RAPIDAPI_KEY=  # Instagram 抓取
JINA_API_KEY=  # 网页读取
```

---

## 开发原则

**先移动端，再桌面端。业务逻辑共享，UI 按需区分。**
